import { createTheme } from "@mui/material";

export const themeFinanzas = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#009688",
    },
    secondary: {
      main: "#FFC107",
    },
  },
});
